<% provide(:title, "New Term") %>
<div class="col-sm-10 col-sm-offset-1 col-md-6 col-md-offset-3 col-xl-4 col-xl-offset-4">
  <h3 class="red-text">Submit a New Term</h3>
  <%= render 'form' %>
  <br>
  
<% if @term.errors.any? %>
    <div id="error_explanation">
      <b><%= pluralize(@term.errors.count, "error") %> prohibited this term from being saved:</b>
      <ul>
      <% @term.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
  </div>